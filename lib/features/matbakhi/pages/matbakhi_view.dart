import 'package:flutter/material.dart';

class MatbakhiView extends StatelessWidget {
  const MatbakhiView({super.key});

  @override
  Widget build(BuildContext context) {
    return  const SingleChildScrollView(child:  Text('matbakhi'));
  }
}
